services:
  azp-agent:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: 
      - path: ./.env
        required: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
